<!doctype html> 
<!--[if lt IE 7]> <html class="no-js ie6 oldie" lang="en"> <![endif]--> 
<!--[if IE 7]>    <html class="no-js ie7 oldie" lang="en"> <![endif]--> 
<!--[if IE 8]>    <html class="no-js ie8 oldie" lang="en"> <![endif]--> 
<!--[if gt IE 8]><!--> <html class="no-js" lang="en"> <!--<![endif]--> 
<head>  
	<meta charset="UTF-8"> 
	<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1"> 
	
	<title>Griddy</title>
	
	<!-- Style -->	
	<% if (cookies[:version] == "high") %> 
			<%= stylesheet_link_tag "global", "screen/shared", "screen/med", "screen/high", :cache => 'cache/main/display' %>
	<% elsif (cookies[:version] == "med") %>
			<%= stylesheet_link_tag "global", "screen/shared", "screen/med", :cache => 'cache/main/display'  %>
	<% else  %>
			<%= stylesheet_link_tag "global", "screen/shared", "screen/low", :cache => 'cache/main/display'  %>
	<% end  %>
	
	<!-- CSRF -->
	<%= csrf_meta_tag %>
	
	<!-- Script -->
	<% 
		cookies[:avail] = false
		if (cookies[:avail] != "true")
			cookies[:avail] = "true"		
		end	
	%>
	<%= javascript_include_tag :defaults %>
		
	<meta name="description" content=""> 
	<meta name="author" content=""> 
	<meta name="viewport" content="width=device-width, initial-scale=1.0"> 
</head> 
<body> 
	<div id="header">
		<h1 class="heading page-title"><a href="/"><span class="hide">Griddy</span></a></h1>
		<ul class="nav">
			<li><%= link_to "Now", grid_now_path unless @talks_taking_place %></li>
			<li><a href="/grid/next">Next</a></li>
			<li><a href="/grid">View Grid</a></li>
			<li><a href="/talks/new">Add Talk</a></li>
			<li><a href="/feedback">Feedback</a></li>
		</ul>
		<% if (cookies[:avail] == "true") %>
			<div class="version-switcher">
				<h2 class="heading">Versions <span class="skip">[<a href="#header-end">skip</a>]</span></h2>
				<p class="options"><a href="<%= url_for() %>" <% if (cookies[:version] != 'med' && cookies[:version] != 'high') %>class="selected" <% end %>onClick="setVersion('low');">Bare</a> | <a href="<%= url_for() %>"<% if (cookies[:version] == 'med') %>class="selected" <% end %> onClick="setVersion('med');">Simple</a> | <a href="<%= url_for() %>"<% if (cookies[:version] == 'high') %>class="selected" <% end %> onClick="setVersion('high');">Shiny</a></p>
			</div>
		<% end %>
		<span id="header-end"></span>		
	</div>
