<% @rooms.each do | room| %>
	<% @room = room %>
	<tr> 
		<th class="room"><p><%= @room.name %></p></th>
		<% @room.slots.each do | slot| %>
			<% @slot = slot %>
			<%= render 'shared/grid/slot' %>
		<% end %> 
	</tr>
<% end %>