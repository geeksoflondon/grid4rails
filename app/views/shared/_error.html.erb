<div id="error_explanation">
	<h2><%= pluralize(@errors.count, "error") %> <%= @context%></h2>
	<ul>
		<% @errors.full_messages.each do |msg| %>
    		<li><%= msg %></li>
  		<% end %>
  	</ul>
</div>