<div class="message <% if (@timeslots.count() < 1) %>warning<% else %>instructions<% end %>">
	<% if (@timeslots.count() < 1) %>
		<p>Wow, it looks as though there's no empty slots to move your talk to!</p>
	<% else %>
		<p>You can add your talk to an empty slot or swap it with another talk.  Once you've chosen your slot, click to select and hit submit.</p>
	<% end %>
	<%= form_tag(:controller => "talks", :action => "unschedule") do |f| %>
		<%= hidden_field('talk', :value => @unscheduled.id) %>
		<fieldset class="actions">
			<p>
				<span class="cancel"><%= link_to "Cancel", url_options = { :controller => "talks", :action => "show", :id => @unscheduled.id } %></span>
				<span class="delimiter"> </span>
				<span class="unschedule"><%= link_to "Remove Talk", url_options = { :controller => "talks", :action => "unschedule", :id => @unscheduled.id } %></span>
			</p>
		</fieldset>
	<% end %>	
</div>
<% if (@version == "small") %>
	<%= render "shared/grid/grid_vertical" %>
<% else %>
	<%= render "shared/grid/grid" %>
<% end %>