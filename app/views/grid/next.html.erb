<div class="grid">
	<h1 class="heading">Next</h1> 
	<%= render '/shared/nav/timeslot' %>
	<% if (@timeslot.nil?) %>
		<p>That's all folks!</p>
	<% else %>
		<%= render 'shared/grid/headings' %>
	<% end %>
</div>